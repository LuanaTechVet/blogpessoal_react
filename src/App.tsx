import Home from './pages/home/Home'

function App() {
    return (
        <Home /> /* para o componente ser renderizado, ele precisa estar inserido dentro de uma tag, semelhante as tags HTML*/
    )
}

export default App

